
description = 'Bootstrap runner'
group 'org.zanata.sync.bootstrap'

jar {
    manifest {
        attributes(
            "Implementation-Title": "Gradle",
            "Implementation-Version": version,
            'Main-Class': 'org.zanata.sync.bootstrap.JettyBootstrap'
        )
    }
}
